{
  "name": "TODO",
  "description": "TODO",
  "version": "0.0.1",
  "scripts": {
    "dev": "sapper dev",
    "build": "sapper build --legacy",
    "export": "sapper export --legacy",
    "start": "node __sapper__/build",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "run-p --race dev cy:run",
    "deploy": "rm -rf dist && sapper export && cd __sapper__/export && git init && git add . && git commit -m 'Build' && git remote add origin git@github.com:sidbentifraouine/sidbentifraouine.github.io.git && git push --force origin master:gh-pages && cd ../.."
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test",
      "pre-push": "npm run deploy"
    }
  },
  "dependencies": {
    "compression": "^1.7.1",
    "husky": "^4.2.5",
    "polka": "next",
    "sirv": "^0.4.0"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "@babel/plugin-syntax-dynamic-import": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "@babel/runtime": "^7.0.0",
    "@rollup/plugin-babel": "^5.0.0",
    "@rollup/plugin-commonjs": "^12.0.0",
    "@rollup/plugin-node-resolve": "^8.0.0",
    "@rollup/plugin-replace": "^2.2.0",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.3.4",
    "rollup-plugin-svelte": "^5.0.1",
    "rollup-plugin-terser": "^7.0.0",
    "sapper": "^0.27.0",
    "svelte": "^3.0.0"
  }
}
